import customtkinter as ctk #Bibliothéque utilisé pour le developpement des interfaces graphiques 

import cv2 #Bibliothéque utilisé pour capturer des images vidéo à partir d'une caméra, retourner des images, redimensionner des images et écrire des fichiers vidé

from PIL import Image, ImageTk #Bibliothéque utilisé pour gérer les images en convertissant les images OpenCV dans un format adapté à l'afficher dans le canevas Tkinter

from datetime import datetime #bibliothèque utilisé pour obtenir la date et l'heure actuelles afin de créer des dossiers et de nommer des fichiers

import os #Bibliothéque utilisé pour des interfaces système d'exploitation diverses, telles que la création de répertoires et la manipulation des chemins de fichiers

import sqlite3 #Bibliothéque utilisé pour exécuter des commandes SQL à partir d'un fichier

import pyaudio ##Bibliothéque utilisé pour faire des enregistrements audio

import wave #Bibliothéque utilisé pour faire enregistrer des enregistrements audio

from moviepy.editor import VideoFileClip, AudioFileClip, concatenate_videoclips #Bibliothéque utilisé pour combiner des clips vidéo et audio en un seul fichier vidéo
